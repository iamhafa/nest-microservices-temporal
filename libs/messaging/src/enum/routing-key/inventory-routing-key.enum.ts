export enum InventoryRoutingKey {
  ADJUST = 'inventory.adjust',
  GET_ALL = 'inventory.get-all',
  GET_AVAILABLE_STOCK = 'inventory.get-available-stock',
  RESERVE = 'inventory.reserve',
  CONFIRM = 'inventory.confirm',
  RESTORE = 'inventory.restore',
}
